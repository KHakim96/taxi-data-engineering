config {
  type: "table",
  schema: "gold",
  name: "shift_summary",
  description: "DEPRECATED (forensic review 2026-08-15) — DO NOT USE. The 8h-gap session + anchored 24h-window model produced misleading semantics: 94.3% of sessions with >12h accumulated active time produced no flagged window (anchored-split evasion), and meter artifacts fabricated up to 69% of 'Potential Overworker' flags. The canonical utilization model is now gold.vehicle_activity_day (Vehicle-Day Utilization). This model is disabled and no longer builds; it is retained only as a historical reference. Nothing in the pipeline depends on it.",
  disabled: true
}

-- Intentionally empty: model disabled.
-- Historical logic (for reference only) lived in git history:
--   sweep-line merge -> 8h-gap sessions -> anchored 24h windows
--   -> 'Potential Overworker' classification.
-- Replaced by: definitions/gold/vehicle_activity_day.sqlx
SELECT 1 AS deprecated_placeholder WHERE FALSE
