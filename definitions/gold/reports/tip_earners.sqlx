config {
  type: "table",
  schema: "gold",
  name: "tip_earners",
  description: "Highest Tip Earners"
}

SELECT

    company,

    SUM(tips) AS total_tips,

    AVG(tips) AS average_tip,

    COUNT(*) AS trips

FROM ${ref("fact_taxi_trip")}

GROUP BY company

ORDER BY total_tips DESC